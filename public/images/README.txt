This folder is referenced by app/layout.tsx for the OpenGraph/Twitter
preview image at /images/og-cover.jpg (1200x630px recommended).

No image is bundled with this build — the hero visual is drawn on canvas
in code, not a static file. Add your own og-cover.jpg here before deploying,
or update the path in app/layout.tsx if you name it differently.
